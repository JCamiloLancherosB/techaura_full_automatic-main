# Lease-Based Worker System - Visual Flow Diagram

## Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         MySQL Database                           │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │              processing_jobs table                          │ │
│  │  ┌──────────┬────────────┬──────────────┬──────────────┐  │ │
│  │  │ id       │ status     │ locked_by    │ locked_until │  │ │
│  │  ├──────────┼────────────┼──────────────┼──────────────┤  │ │
│  │  │ 1        │ pending    │ NULL         │ NULL         │  │ │
│  │  │ 2        │ processing │ worker-1     │ 2026-01-...  │  │ │
│  │  │ 3        │ retry      │ NULL         │ NULL         │  │ │
│  │  │ 4        │ done       │ NULL         │ NULL         │  │ │
│  │  └──────────┴────────────┴──────────────┴──────────────┘  │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                              ▲
                              │
                              │ Atomic UPDATE queries
                              │
┌─────────────────────────────┼─────────────────────────────┐
│                             │                              │
│  ┌──────────────────────────┴───────────┐   ┌────────────────────┐
│  │        Worker 1                       │   │    Worker 2        │
│  │  ┌─────────────────────────────────┐ │   │  ┌───────────────┐ │
│  │  │  1. Poll for jobs (every 5s)    │ │   │  │  1. Poll      │ │
│  │  │  2. Acquire lease atomically    │ │   │  │  2. Acquire   │ │
│  │  │  3. Process job                 │ │   │  │  3. Process   │ │
│  │  │  4. Extend lease if needed      │ │   │  │  4. Extend    │ │
│  │  │  5. Release lease on done       │ │   │  │  5. Release   │ │
│  │  └─────────────────────────────────┘ │   │  └───────────────┘ │
│  └──────────────────────────────────────┘   └────────────────────┘
└─────────────────────────────────────────────────────────────────┘
```

## Job Lifecycle Flow

```
                        ┌──────────────┐
                        │   Job Created│
                        │ status=pending│
                        └───────┬──────┘
                                │
                                ▼
                        ┌──────────────┐
                  ┌────►│   PENDING    │◄────┐
                  │     │              │     │ On worker
                  │     └───────┬──────┘     │ startup
                  │             │            │ (expired lease)
                  │             │ Worker     │
                  │             │ acquires   │
                  │             │ lease      │
                  │             ▼            │
                  │     ┌──────────────┐     │
                  │     │ PROCESSING   │─────┘
                  │     │ (locked)     │
                  │     └───────┬──────┘
                  │             │
                  │             │ Job completes
                  │             │ or fails
                  │             │
                  │     ┌───────┴──────┐
                  │     │              │
                  │     ▼              ▼
            ┌─────┴──────┐      ┌──────────┐
            │   RETRY    │      │   DONE   │
            │ (if att<3) │      │          │
            └─────┬──────┘      └──────────┘
                  │
                  │ 3 attempts
                  │ exhausted
                  │
                  ▼
            ┌──────────┐
            │  FAILED  │
            │ (att>=3) │
            └──────────┘
```

## Lease Acquisition Flow (Atomic)

```
Worker wants to process a job:

1. Begin Transaction
   │
   ▼
2. Execute atomic UPDATE:
   ┌──────────────────────────────────────────────────┐
   │ UPDATE processing_jobs                           │
   │ SET locked_by = 'worker-1'                       │
   │     locked_until = NOW() + 300 seconds           │
   │     status = 'processing'                        │
   │     attempts = attempts + 1                      │
   │ WHERE id = (                                     │
   │   SELECT id FROM (                               │
   │     SELECT id                                    │
   │     FROM processing_jobs                         │
   │     WHERE status IN ('pending', 'retry')         │
   │       AND (locked_until IS NULL                  │
   │            OR locked_until < NOW())              │
   │       AND attempts < 3                           │
   │     ORDER BY created_at ASC                      │
   │     LIMIT 1                                      │
   │   ) AS subquery                                  │
   │ )                                                │
   └──────────────────────────────────────────────────┘
   │
   ▼
3. Check affected rows:
   │
   ├─ If 1 row affected → Job acquired ✅
   │  └─ Commit transaction
   │     └─ Start processing
   │
   └─ If 0 rows affected → No jobs available ❌
      └─ Rollback transaction
         └─ Wait and try again
```

## Crash Recovery Flow

```
Worker crashes while processing job:

Before Crash:
┌──────────────────────────────────────┐
│ Job ID: 123                          │
│ Status: processing                   │
│ Locked By: worker-1                  │
│ Locked Until: 2026-01-23 21:30:00   │
│ Attempts: 1                          │
└──────────────────────────────────────┘
         │
         │  CRASH! 💥
         │  (worker-1 dies)
         │
         ▼  Time passes...
         │  (lease expires)
         │
         ▼
┌──────────────────────────────────────┐
│ Job ID: 123                          │
│ Status: processing                   │
│ Locked By: worker-1                  │
│ Locked Until: 2026-01-23 21:30:00   │ ◄── Expired!
│ Attempts: 1                          │
└──────────────────────────────────────┘
         │
         │  Worker restarts
         │
         ▼
   resetExpiredLeases()
         │
         ▼
┌──────────────────────────────────────┐
│ Job ID: 123                          │
│ Status: retry                        │ ◄── Reset to retry
│ Locked By: NULL                      │ ◄── Lease released
│ Locked Until: NULL                   │
│ Attempts: 1                          │
│ Last Error: "Lease expired - ..."   │ ◄── Error logged
└──────────────────────────────────────┘
         │
         │  Job available again
         │
         ▼
   Worker acquires job
         │
         ▼
┌──────────────────────────────────────┐
│ Job ID: 123                          │
│ Status: processing                   │ ◄── Processing again
│ Locked By: worker-2                  │ ◄── New worker
│ Locked Until: 2026-01-23 21:35:00   │ ◄── New lease
│ Attempts: 2                          │ ◄── Incremented
└──────────────────────────────────────┘
         │
         │  Job completes
         │
         ▼
┌──────────────────────────────────────┐
│ Job ID: 123                          │
│ Status: done                         │ ◄── Success!
│ Locked By: NULL                      │
│ Locked Until: NULL                   │
│ Attempts: 2                          │
│ Finished At: 2026-01-23 21:33:45    │
└──────────────────────────────────────┘
```

## Retry Logic Flow

```
Job Processing Attempts:

Attempt 1:
┌────────────┐
│ attempts=0 │  → Processing → ❌ Fails
└────────────┘                  │
                                ▼
Attempt 2:                ┌────────────┐
┌────────────┐           │ attempts=1 │
│ status=    │◄──────────┤ status=    │
│   retry    │           │   retry    │
└────────────┘           └────────────┘
      │                        │
      │  → Processing → ❌ Fails│
      ▼                        ▼
Attempt 3:                ┌────────────┐
┌────────────┐           │ attempts=2 │
│ status=    │◄──────────┤ status=    │
│   retry    │           │   retry    │
└────────────┘           └────────────┘
      │                        │
      │  → Processing → ❌ Fails│
      ▼                        ▼
┌────────────┐           ┌────────────┐
│ attempts=3 │           │ status=    │
│ status=    │──────────►│   failed   │
│   failed   │           │ PERMANENT  │
└────────────┘           └────────────┘
                         (no more retries)
```

## Concurrent Worker Scenario

```
Timeline showing two workers trying to process same job:

Time    Worker 1                    Worker 2
──────────────────────────────────────────────────────
T0      Poll for jobs               Poll for jobs
        │                           │
T1      ├─ Find job #123            ├─ Find job #123
        │  (status=pending)         │  (status=pending)
        │                           │
T2      ├─ Execute UPDATE           ├─ Execute UPDATE
        │  (begin transaction)      │  (begin transaction)
        │                           │
T3      ├─ Lock acquired ✅          ├─ Lock NOT acquired ❌
        │  (1 row affected)         │  (0 rows affected)
        │                           │
T4      ├─ Commit transaction       ├─ Rollback
        │                           │
T5      ├─ Start processing         └─ Wait for next poll
        │                           
T6      ├─ Processing...            Poll for jobs
        │                           │
T7      ├─ Processing...            ├─ Find job #124
        │                           │  (different job)
T8      ├─ Complete ✅               ├─ Start processing #124
        │                           │
        └─ Release lease            └─ Processing...

Result: No conflict! Each worker processes different job.
```

## Lease Extension for Long Jobs

```
Long-running job processing:

Start                        50% threshold              Lease expires
│                                  │                          │
│  ┌────────────────────────────┐ │ ┌────────────────────────┤
│  │  Job Processing            │ │ │                        │
│  │  Lease: 300 seconds        │ │ │                        │
│  └────────────────────────────┘ │ └────────────────────────┤
│                                  │                          │
├─────────────┬───────────────────┼──────────────────────────┤
              │                   │                          │
              │                   ▼                          │
              │          ┌──────────────────┐               │
              │          │ Extend Lease     │               │
              │          │ +300 seconds     │               │
              │          └──────────────────┘               │
              │                   │                          │
              │                   ├──────────────────────────┼────────►
              │                   │                          │   New lease
              │                   │                          │   expiration
              ▼                   ▼                          ▼
         Processing          Processing                Processing
         continues           continues                 continues
                                                       safely
```

## Error Handling Flow

```
┌─────────────────────┐
│  Job Processing     │
└──────────┬──────────┘
           │
           │
    ┌──────┴──────┐
    │             │
    ▼             ▼
┌─────────┐   ┌────────────┐
│ Success │   │   Error    │
└────┬────┘   └─────┬──────┘
     │              │
     │              │ Log error
     │              │
     │              ▼
     │        ┌──────────────┐
     │        │ attempts < 3?│
     │        └──────┬───┬───┘
     │               │   │
     │          Yes  │   │  No
     │               │   │
     │               ▼   ▼
     │        ┌───────┐ ┌────────┐
     │        │ retry │ │ failed │
     │        └───────┘ └────────┘
     │               │
     ▼               │
┌─────────┐         │
│  done   │◄────────┘
└─────────┘

All paths release lease with appropriate status
```

## Key Benefits Illustrated

```
╔════════════════════════════════════════════════════╗
║           Traditional Approach Problems            ║
╠════════════════════════════════════════════════════╣
║  ❌ Race Conditions:                               ║
║     Two workers process same job                   ║
║                                                    ║
║  ❌ Lost Jobs:                                     ║
║     Worker crashes, job never retried             ║
║                                                    ║
║  ❌ Duplicate Orders:                              ║
║     Same job processed multiple times              ║
║                                                    ║
║  ❌ Manual Recovery:                               ║
║     Admin must manually reset stuck jobs          ║
╚════════════════════════════════════════════════════╝

        ┌────────────────────────┐
        │   Lease-Based System   │
        └───────────┬────────────┘
                    │
                    ▼

╔════════════════════════════════════════════════════╗
║              Solutions Provided                    ║
╠════════════════════════════════════════════════════╣
║  ✅ Atomic Locking:                                ║
║     Database transactions prevent conflicts        ║
║                                                    ║
║  ✅ Auto Recovery:                                 ║
║     Expired leases reset automatically            ║
║                                                    ║
║  ✅ No Duplication:                                ║
║     One worker owns each job at a time            ║
║                                                    ║
║  ✅ Self-Healing:                                  ║
║     System recovers from crashes automatically     ║
╚════════════════════════════════════════════════════╝
```

## Monitoring Dashboard Concept

```
┌─────────────────────────────────────────────────────────┐
│  Lease-Based Job Processing - Live Dashboard           │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  Active Workers: 3                                      │
│  ┌──────────────┬─────────────┬──────────────────────┐ │
│  │ worker-1     │ 2 jobs      │ Healthy ✅           │ │
│  │ worker-2     │ 1 job       │ Healthy ✅           │ │
│  │ worker-3     │ 0 jobs      │ Idle                 │ │
│  └──────────────┴─────────────┴──────────────────────┘ │
│                                                         │
│  Job Queue Status:                                      │
│  ┌──────────────┬─────────────────────────────────────┐│
│  │ Pending      │ ████████ 8 jobs                     ││
│  │ Processing   │ ███ 3 jobs                          ││
│  │ Retry        │ ██ 2 jobs                           ││
│  │ Done (today) │ ████████████████ 156 jobs          ││
│  │ Failed       │ █ 1 job                             ││
│  └──────────────┴─────────────────────────────────────┘│
│                                                         │
│  ⚠️  Alerts:                                            │
│  └─ No expired leases detected ✅                       │
│                                                         │
│  Recent Completions:                                    │
│  ┌──────┬─────────────────────────────────────────────┐│
│  │ #125 │ Completed in 2m 34s                         ││
│  │ #124 │ Completed in 1m 12s                         ││
│  │ #123 │ Completed in 3m 45s (2 attempts)            ││
│  └──────┴─────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────┘
```
